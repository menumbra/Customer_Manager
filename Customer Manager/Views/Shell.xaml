<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Customer_Manager.Views.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Customer_Manager.Views"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <controls:NavigationView
  x:Name="NavView"
  PaneDisplayMode="Auto"
  CompactModeThresholdWidth="800"
  ExpandedModeThresholdWidth="1008"
  IsBackButtonVisible="Collapsed"
  IsSettingsVisible="False"
  SelectionChanged="NavView_SelectionChanged"
  IsTitleBarAutoPaddingEnabled="True"      
  Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <!-- Header -->
    <controls:NavigationView.Header>
      <Grid Height="32" VerticalAlignment="Center">
        <TextBlock Text="Customer Manager"
                   VerticalAlignment="Center"
                   Margin="12,0,0,0"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="{ThemeResource TextFillColorPrimary}" />
      </Grid>
    </controls:NavigationView.Header>

    <!-- Menu Items -->
    <controls:NavigationView.MenuItems>
      <controls:NavigationViewItem Icon="Contact" Content="Customers" Tag="customers"/>
    </controls:NavigationView.MenuItems>

    <!-- Footer Items -->
    <controls:NavigationView.PaneFooter>
      <controls:NavigationViewItem Icon="Setting" Content="Settings" Tag="settings"/>
    </controls:NavigationView.PaneFooter>

    <!-- Content Frame -->
    <Frame x:Name="ContentFrame"/>
  </controls:NavigationView>
</Window>


