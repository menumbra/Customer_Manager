<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Customer_Manager.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Customer_Manager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="Customer Manager">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <StackPanel Padding="20" Spacing="10">
        <TextBox x:Name="NameTextBox" Header="Customer Name" />
        <TextBox x:Name="EmailTextBox" Header="Email Address" KeyDown="EmailTextBox_KeyDown" />

        <Button x:Name="AddButton" Content="Add Customer" Click="AddCustomer_Click" />
        <Button x:Name="DeleteButton" Content="Delete Selected" Click="DeleteCustomer_Click" />
        <Button x:Name="RefreshButton" Content="🔄 Refresh List" Click="RefreshButton_Click" />

        <toolkit:DataGrid
            x:Name="CustomerDataGrid"
            AutoGenerateColumns="False"
            Margin="0,20,0,0"
            Height="300">

            <toolkit:DataGrid.Columns>
                <toolkit:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <toolkit:DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                <toolkit:DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="Auto" />
                <toolkit:DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="Auto" />
                <toolkit:DataGridTextColumn Header="Editor" Binding="{Binding Editor}" Width="Auto" />
            </toolkit:DataGrid.Columns>
        </toolkit:DataGrid>
    </StackPanel>
</Window>
